const initialState = [{
    isFetching: false,
    wordChoice: '',
    wordChars: [{
        char: '',
        show: flase
    }]
}];

export const WordReducer = (state = initialState, action) => {
    switch (action.type) {
        case FIND_CHAR:

    }
}